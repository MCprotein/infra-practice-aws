import tw from 'tailwind-styled-components';

export const CommentPostArea = tw.textarea`
    textarea 
    textarea-bordered 
    textarea-xs 
    min-h-0
    h-10 
    w-full 
    max-w-xs 
    rounded-full
`;
